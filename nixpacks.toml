[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python -m gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --timeout 300"
